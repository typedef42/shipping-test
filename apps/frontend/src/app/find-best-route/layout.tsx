export default function FindBestRouteLayout({ children }: { children: React.ReactNode }) {
  return <section className="flex flex-col items-center justify-center min-h-screen py-2 min-w-20">{children}</section>;
}
